<%= form_for(@eventodepo) do |f| %>
  <% if @eventodepo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eventodepo.errors.count, "error") %> prohibited this eventodepo from being saved:</h2>

      <ul>
      <% @eventodepo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :premio %><br />
    <%= f.text_field :premio %>
  </div>
  <div class="field">
    <%= f.label :cantidad %><br />
    <%= f.number_field :cantidad %>
  </div>
  <div class="field">
    <%= f.label :plazoinsc %><br />
    <%= f.datetime_select :plazoinsc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
